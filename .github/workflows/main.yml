name: build
on:
  push:
    branches:
      - master
jobs:
  build-app:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2.4.0
      - uses: actions/setup-python@v2
      - run: flutter build web
      - run: python copy_build.py
